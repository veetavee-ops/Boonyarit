services:
  app:
    image: sotuseng/lineoa:${IMAGE_TAG:-latest}
    container_name: lineoa-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    volumes:
      - lineoa-media:/app/media

volumes:
  lineoa-media:
